# Team Workflow upstream touch points

Fork-only implementation lives under adjacent `fork/` directories. This is the checklist for replaying the remaining wiring after an upstream merge. `PROGRESS.md` and the original feature plan are archived under `docs/fork/archived/`.

| Upstream file                                                       | What changed and why it remains                                                                                                                                          |
| ------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `apps/server/integration/OrchestrationEngineHarness.integration.ts` | Stubs the Team Workflow report reactor because the shared integration layer now requires it.                                                                             |
| `apps/server/src/environment/ServerEnvironment.ts`                  | Advertises the Team Workflow capability to clients.                                                                                                                      |
| `apps/server/src/mcp/McpHttpServer.ts`                              | Registers fork-owned Team Workflow MCP tools in the existing MCP server layer.                                                                                           |
| `apps/server/src/mcp/McpInvocationContext.ts`                       | Adds the `team` capability to the existing credential capability union.                                                                                                  |
| `apps/server/src/mcp/McpProviderSession.ts`                         | Carries optional Team Workflow prompt context across the existing provider-session boundary.                                                                             |
| `apps/server/src/orchestration/Layers/OrchestrationReactor.test.ts` | Wires a fork-owned reactor stub into the existing start-order test and includes the reactor in its expected order. This is necessary for the upstream test to run.       |
| `apps/server/src/orchestration/Layers/OrchestrationReactor.ts`      | Starts the fork-owned Team Workflow report reactor alongside upstream reactors.                                                                                          |
| `apps/server/src/orchestration/Layers/ProjectionPipeline.ts`        | Carries optional Team Workflow metadata into the existing thread projection.                                                                                             |
| `apps/server/src/orchestration/Layers/ProjectionSnapshotQuery.ts`   | Reads the `team_json` column in existing shell/detail SQL queries and delegates optional-field mapping to fork code. These SQL row shapes cannot be extended externally. |
| `apps/server/src/orchestration/decider.ts`                          | Copies optional Team Workflow metadata from thread-create command to creation event.                                                                                     |
| `apps/server/src/orchestration/projector.ts`                        | Copies optional Team Workflow metadata from creation event to read model.                                                                                                |
| `apps/server/src/persistence/Layers/ProjectionThreads.ts`           | Reads and writes `team_json` in the existing thread projection repository; the existing SQL statement must include the column.                                           |
| `apps/server/src/persistence/Migrations.ts`                         | Registers fork migration 54, which reconciles the upstream and fork migration-53 histories.                                                                              |
| `apps/server/src/persistence/Services/ProjectionThreads.ts`         | Extends the existing projection row schema with optional Team Workflow metadata.                                                                                         |
| `apps/server/src/provider/CodexDeveloperInstructions.ts`            | Passes optional Team Workflow context into Codex developer instructions.                                                                                                 |
| `apps/server/src/provider/Layers/AntigravityAdapter.ts`             | Carries Team Workflow context into Antigravity's existing prompt path.                                                                                                   |
| `apps/server/src/provider/Layers/ClaudeAdapter.ts`                  | Carries Team Workflow context into Claude's existing prompt path.                                                                                                        |
| `apps/server/src/provider/Layers/CodexAdapter.ts`                   | Passes Team Workflow context to the Codex session runtime.                                                                                                               |
| `apps/server/src/provider/Layers/CodexSessionRuntime.ts`            | Carries Team Workflow context into Codex turn-start instructions.                                                                                                        |
| `apps/server/src/provider/Layers/CursorAdapter.ts`                  | Carries Team Workflow context into Cursor's existing prompt path.                                                                                                        |
| `apps/server/src/provider/Layers/GrokAdapter.ts`                    | Carries Team Workflow context into Grok's existing prompt path.                                                                                                          |
| `apps/server/src/provider/Layers/OpenCodeAdapter.ts`                | Carries Team Workflow context into OpenCode's existing prompt path.                                                                                                      |
| `apps/server/src/provider/Layers/ProviderService.ts`                | Calls fork-owned capability and prompt resolvers and normalizes the planning command before provider dispatch.                                                           |
| `apps/server/src/provider/RuntimeInstructions.ts`                   | Appends fork-owned Team Workflow instructions to the existing common runtime prompt.                                                                                     |
| `apps/server/src/server.ts`                                         | Wires the fork worker bootstrap, branch integration, and report reactor into server layers.                                                                              |
| `apps/server/src/serverSettings.ts`                                 | Wires fork-owned Team Workflow defaults and settings normalization into load and save paths.                                                                             |
| `apps/server/src/ws.ts`                                             | Preserves optional Team Workflow metadata in the existing thread bootstrap RPC.                                                                                          |
| `apps/server/vite.config.ts`                                        | Loads the fork planning skill Markdown as text in tests and server bundles, keeping one installable source.                                                              |
| `apps/web/src/components/ChatView.tsx`                              | Wires Team Workflow selection, draft updates, thread creation, header/composer props, and the fork-owned team panel into the chat surface.                               |
| `apps/web/src/components/CommandPalette.tsx`                        | Registers the fork-owned orchestrator action when a Team Workflow is available.                                                                                          |
| `apps/web/src/components/RightPanelTabs.tsx`                        | Adds the Team Workflow surface to existing tab, launcher, and menu unions; action construction lives in fork code.                                                       |
| `apps/web/src/components/Sidebar.tsx`                               | Wires the fork-owned worker badge and orchestrator-thread action into existing sidebar entry points.                                                                     |
| `apps/web/src/components/chat/ChatComposer.tsx`                     | Adds a slot and props for the fork-owned Team Workflow picker and draft placeholder, plus the planning command suggestion.                                               |
| `apps/web/src/components/chat/ChatHeader.tsx`                       | Renders fork-owned worker navigation and role badge.                                                                                                                     |
| `apps/web/src/components/chat/CompactComposerControlsMenu.tsx`      | Renders fork-owned Team Workflow menu choices.                                                                                                                           |
| `apps/web/src/components/chat/MessagesTimeline.tsx`                 | Routes Team Workflow report messages to a fork-owned timeline row.                                                                                                       |
| `apps/web/src/components/settings/SettingsSidebarNav.tsx`           | Registers the Team Workflow settings icon in existing navigation.                                                                                                        |
| `apps/web/src/components/settings/settingsSearch.ts`                | Registers the fork-owned Team Workflow settings search item and route metadata.                                                                                          |
| `apps/web/src/components/sidebar/SidebarThreadHeader.tsx`           | Exposes the orchestrator-thread action in the existing sidebar header.                                                                                                   |
| `apps/web/src/composerDraftStore.ts`                                | Extends persisted draft state with optional Team Workflow selection; existing draft hydration and equality must carry it.                                                |
| `apps/web/src/hooks/useHandleNewThread.ts`                          | Passes Team Workflow selection through each existing draft creation/reuse path.                                                                                          |
| `apps/web/src/lib/chatThreadActions.ts`                             | Extends the existing new-thread handler option type for Team Workflow drafts.                                                                                            |
| `apps/web/src/rightPanelStore.ts`                                   | Adds the Team Workflow surface to the persisted panel union and bumps its storage version.                                                                               |
| `apps/web/src/routeTree.gen.ts`                                     | Generated TanStack route registration for the Team Workflow settings shim; generator output cannot carry stable `FORK:` comments.                                        |
| `apps/web/src/routes/_chat.tsx`                                     | Wires the Team Workflow keybinding into global chat shortcuts.                                                                                                           |
| `apps/web/src/routes/settings.workflows.tsx`                        | File-router shim required in the upstream routes directory; it renders the fork-owned settings panel.                                                                    |
| `packages/client-runtime/package.json`                              | Exports the fork-owned shared Team Workflow state subpath; JSON cannot carry a `FORK:` comment.                                                                          |
| `packages/contracts/src/environment.ts`                             | Adds an optional Team Workflow capability field to the existing environment schema.                                                                                      |
| `packages/contracts/src/index.ts`                                   | Exports fork-owned Team Workflow wire schemas.                                                                                                                           |
| `packages/contracts/src/keybindings.ts`                             | Adds the Team Workflow shortcut command to the existing command list.                                                                                                    |
| `packages/contracts/src/orchestration.ts`                           | Adds optional Team Workflow metadata to existing thread/create/bootstrap/event shapes; optional fields preserve existing consumers.                                      |
| `packages/contracts/src/settings.ts`                                | Adds Team Workflow presets to settings and patch schemas.                                                                                                                |
| `packages/shared/package.json`                                      | Exports the fork-owned built-in Team Workflow preset subpath; JSON cannot carry a `FORK:` comment.                                                                       |
| `packages/shared/src/serverSettings.ts`                             | Applies Team Workflow settings patches in the existing shared patch function.                                                                                            |

All standalone Team Workflow tests live under `fork/`. The upstream reactor start-order test retains only the wiring required to test the extended reactor list. No upstream file was renamed, moved, or deleted.

## Known Windows test failure

`apps/server/src/mcp/McpHttpServer.test.ts:341` fails in "saves the snapshot PNG on request and reports its path". The assertion searches JSON response text for the raw Windows screenshot path, but JSON escapes its backslashes. The test file matches upstream at the fork point. This restructure did not change the assertion. When revisiting it, parse the JSON and compare `screenshotPath`. No fix was made here.
